{{- range .Values.remoteMcpServers }}
apiVersion: kagent.dev/v1alpha2
kind: RemoteMCPServer
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.namespace }}
spec:
  transport: {{ .transport }}
  url: {{ .url | quote }}
---
{{- end }}
