image:
  tag: ""
workload:
  name: feast-offline-writer
  command: [python, scripts/feast/run_offline_writer.py]
deploymentStrategy:
  type: RollingUpdate
  rollingUpdate: { maxUnavailable: 0, maxSurge: 1 }
resources:
  requests: { cpu: 100m, memory: 256Mi }
  limits: { cpu: 500m, memory: 512Mi }
autoscaling:
  enabled: true
  minReplicaCount: 1
  maxReplicaCount: 2
  pollingInterval: 15
  cooldownPeriod: 60
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus-server.edai2.svc.cluster.local
        metricName: edai2_feature_updates_offline_lag
        threshold: "1"
        query: sum(edai2_feature_updates_offline_lag)
streaming:
  topic: customer_feature_updates.v1
  consumerGroup: edai2-feast-offline-writer-v1
  destination: offline
