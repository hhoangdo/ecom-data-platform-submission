apiVersion: v1
kind: Config
clusters:
  - name: render-only
    cluster: { server: https://127.0.0.1:65535 }
contexts:
  - name: render-only
    context: { cluster: render-only, user: render-only }
current-context: render-only
users:
  - name: render-only
    user: {}
