# Section 03 Spark Gold and Parity Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plan Task 6: render and test seven parameterized Spark Gold relations and prove keyed full-row parity against dbt and generator evidence.

**Architecture:** One parsed generator configuration creates `Section03SqlParameters` for Spark query rendering and exact keyed parity reports against generator/dbt outputs.

**Tech Stack:** Python 3.12, Spark SQL, DuckDB/Trino test doubles, pytest, `uv`, Make, `rtk`.

## Locked sources and acceptance procedure

- Read `C:\Users\oou1hc\.codex\RTK.md` before every operator session and prefix every shell command with `rtk`.
- Locked Section03 source: `tmp/edai2-plan/03_data_generator_improvement.md`, SHA256 `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`.
- Locked EDAI2 source: `tmp/edai2-plan/04.2_llm_design.md`, SHA256 `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`.
- Locked rubric source: `tmp/rubic-check/Coursework Tracking (Public).xlsx`, SHA256 `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`.
- Preflight and final acceptance both run `rtk git status --short --branch`; record the output and reject a branch change during the serial session.
- Final acceptance also records SHA256s, evidence/screenshot QA, cleanup/runtime release, limitations, and successor handoff in the Completion Record.
- Keep one current branch and one serial session; any Kubernetes action sets `KUBECONFIG` and `--context` explicitly, never the corporate current context.
- Kind is local smoke only and never GKE/rubric evidence; do not run Docker prune or stop unrelated containers.

## Metadata

| Field | Decision |
|---|---|
| Phase | 4 — analytic parity |
| Source tasks | Section03 Task 6 |
| Rubric contribution | Supporting evidence for `Sheet3!E34` only; Topic 07 is sole primary owner |
| Prerequisites | Topic 03 Completion Record and candidate manifest identity |
| Blocked successors | Topic 05 only |
| Runtime ownership | Spark/parity operator; serial session |
| Local/GCP class | Local/mock Spark test; no GCP |

## Architecture and technology

The Spark stage accepts generator config/scale, calculates `Section03SqlParameters` once, and renders the same seven DP3 tables in dependency order. In strict Section 03 mode, `scripts/spark/run_batch.py` derives `start_ts`, `end_ts`, feature cutoff, label end, drift start, and baseline date from that parsed config and scale. Explicit `--start-ts`/`--end-ts` values are rejected unless they exactly match those derived values. Parity compares keyed full rows—generator versus dbt and generator versus Spark—not only aggregates. Float persistence is normalized at twelve decimal places with absolute tolerance `1e-9`; labels/keys/timestamps/strings are exact.

## Global constraints

The three locked hashes above remain authoritative. Work on the current branch in one serial session; do not create a worktree or branch and do not stage, commit, push, or open a PR. `REQUIRED_GOLD_TABLES` has no duplicates; core and DP3 sets are disjoint. No hard-coded cutoff/threshold/horizon. Operators use `rtk uv run`; Make recipes are `uv run`. Do not invoke GKE/GCP, prune Docker, or stop unrelated containers.

## Current-state refresh — read-only planning phase

```text
rtk git branch --show-current
rtk git status --short --branch
rtk uv run pytest tests/unit/test_spark_batch_runtime.py tests/unit/test_optional_duckdb_imports.py -q
rtk rg -n "GOLD_SERVING_TABLES|REQUIRED_GOLD_TABLES|ordered_core_gold_queries" src scripts tests
```

Expected: existing query order and test baseline are recorded without writing tables.

## Scope and non-goals

In scope: parameter propagation, SQL rendering, seven-table registration, full-row parity tests/reports. Out of scope: real cluster provisioning, running Airflow/DataHub, chart deployment, and modifying generator public APIs.

## Exact file map

| Action | Path |
|---|---|
| Modify | `src/vina_bim_shop/lakehouse/spark/sql.py`, `src/vina_bim_shop/lakehouse/spark/constants.py`, `src/vina_bim_shop/lakehouse/spark/job.py`, `src/vina_bim_shop/lakehouse/spark/runner.py`, `src/vina_bim_shop/lakehouse/spark/parity.py` |
| Modify | `scripts/spark/run_batch.py` |
| Modify | `tests/unit/test_spark_batch_runtime.py` |
| Modify | `tests/unit/test_optional_duckdb_imports.py` |

## Interfaces, data flow, and failure modes

Config/scale → derived batch/window timestamps and `Section03SqlParameters` → rendered Spark queries → seven persisted outputs → keyed comparison report. Fail on missing config/scale, an explicit batch timestamp that differs from the config-derived value, supplied cutoff not present in SQL, leakage predicates absent, inaccurate percentile shortcut, missing/extra key, nonexact label, nonfinite PSI, tolerance violation, or a candidate manifest/hash mismatch.

## Ordered test-first execution tasks

- [ ] Add failing query-order/leakage, strict config-derived batch-window, explicit mismatch, and four keyed full-row comparison tests, then run `rtk uv run pytest tests/unit/test_spark_batch_runtime.py -q`; expected FAIL because parameterized DP3 queries and strict batch semantics are absent.
- [ ] Thread `--generator-config` and `--generator-scale` through `job.py`, `runner.py`, `scripts/spark/run_batch.py`, and submit construction, then run `rtk uv run pytest tests/unit/test_spark_batch_runtime.py -q`; expected FAIL only on missing seven-query/table/parity contracts while config, scale, and derived start/end/cutoff metadata pass.
- [ ] Implement the seven exact Spark queries, type-7 PSI, ordered `DP3_GOLD_TABLES`, extended `GOLD_SERVING_TABLES`, and duplicate-free `REQUIRED_GOLD_TABLES`, then run `rtk uv run pytest tests/unit/test_spark_batch_runtime.py -q`; expected FAIL only on missing keyed parity report assertions.
- [ ] Implement generator-vs-dbt, generator-vs-Spark, and dbt-vs-Spark reports; production passes validated runtime values equivalent to `scripts/analytics/run_section03_dbt.py --project-dir infra/analytics/dbt --profiles-dir infra/analytics/dbt --config configs/generator/base.yaml --scale medium` as a direct argument array, then run `rtk uv run pytest tests/unit/test_spark_batch_runtime.py -q`; expected PASS with separate zero mismatch counts and bound manifest/config hashes.
- [ ] Run `rtk uv run pytest tests/unit/test_spark_batch_runtime.py tests/unit/test_optional_duckdb_imports.py -q`; expected PASS with cutoff-safe SQL, derived timestamps, ordered seven-table persistence, and no optional DuckDB import regression.
- [ ] Run `rtk git diff --check`, `rtk git status --short --branch`, and `rtk git ls-files --stage`; expected no whitespace errors, the original branch unchanged, only exact file-map paths changed, and the final index listing is byte-for-byte identical to the pre-topic listing. Pre-existing staged entries are user-owned; do not stage or unstage them.

## Evidence and screenshot ownership

The parity operator owns command logs and sanitized parity report containing candidate/config hashes and both comparison families. This is machine proof for finalization, not a GKE screenshot.

## Cleanup

Clean only session-owned local/mock tables and reports after hashes are retained; do not delete canonical bundles or unrelated Spark resources.

## Rubric table

| Cell | Parity contribution | Final scoring |
|---|---|---|
| Sheet3!E34 | Exact label key/values and training join | Section03 strict root required |

## Definition of Done

The seven outputs render in required order, all leakage/PSI parity tests pass, and a hash-bound no-mismatch report is available to runtime promotion.

## Completion Record

### Strict-v4 observed-success completion - 2026-08-06

This entry is authoritative for Topic 04 and supersedes every earlier Completion Record entry where they conflict. The fresh strict-v4 command returned directly with exit `0`, its own artifacts satisfy the complete Spark/Iceberg/three-way-parity contract, and all cleanup, restoration, repository, branch, and index gates pass.

| Field | Result |
|---|---|
| Status | **Complete.** The exact locked-window `rtk uv run python scripts/spark/run_batch.py ...` invocation against the initially absent `tmp/section03-runtime/spark-strict-v4` returned directly with exit `0` after 1,839.8 seconds and printed `Spark batch completed for 2026-03-03T23:59:00Z -> 2026-05-01T23:59:00Z (backfill).` Its own strict artifacts prove all 26 required Gold relations, all seven ordered DP3 relations, PySpark and GX success, successful Trino queries, a 26-table executive mart, empty relation errors, all 43 aggregate/relation comparisons successful, and four zero-mismatch keyed comparisons in each of `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark`. No Topic 04 completion gate remains; Topic 05 is unblocked. |
| Identity and locked inputs | The continuation stayed on `feature/implement-edai2` at `2fbd85ec5899475bf1dd65386263ed2651dc993d`; the starting and final index contain the same 828 tracked entries. Locked SHA-256 values matched: Section 03 plan `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`; candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`. Manifest verification returned `section03 manifest: PASS (runtime pending)` before execution. No branch/worktree operation, stage, commit, push, install, Kubernetes, Kind, GCP, certificate, Spark resource, tolerance, rounding, or source-data change occurred. |
| Implemented correction scope | The unstaged correction set explicitly clears inherited upper- and lower-case HTTP/HTTPS/ALL proxy variables for Spark master, worker, and history plus Trino coordinator and worker, with internal-only `NO_PROXY`/`no_proxy` names. Compose contract tests cover all five services. `_normalise_scalar` canonicalizes only valid strings ending `Z` or ` UTC` to UTC ISO form; ordinary and invalid strings retain exact comparison semantics. The existing `_run_command` captured-output re-emission contract remains green. No SQL, generator, dbt, public interface, table inventory, memory/core, certificate, image, tolerance, or twelve-decimal normalization contract changed. |
| Preflight and owned runtime | The focused preflight returned `47 passed`. The v4 root and `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-v4-20260806` backup path were absent; no project container or volume existed; host, Docker, disk, image, raw-input, and predecessor-state gates passed. The verified backup contained DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, and one dbt log. `rtk make up-batch` returned `0`; both init services exited `0` and all eight long-running services became healthy. The first temporary uploader call only repeated the service's hard-coded bucket entrypoint and landed no inputs; the corrected explicit `/bin/sh` entrypoint invocation then uploaded all ten batch snapshots, four required event topics, and the optional dead-letter object. Independent `mc stat` checks returned `BRONZE_REQUIRED_INVENTORY=PASS`. Spark master/worker/history and Trino coordinator/worker each returned `INTERNAL_PROXY_PREFLIGHT=PASS`; a real Bronze S3A object read returned `SPARK_S3A_BRONZE_PREFLIGHT=PASS 409549`. |
| Strict-v4 application proof | `SECTION03_SPARK_PARITY=PASS gold=26 dp3=7 comparisons=43 keyed=12 mart_rows=539187 artifacts=12 files=14` independently asserted the exact `REQUIRED_GOLD_TABLES` order, the final seven entries equal `DP3_GOLD_TABLES`, 26 Gold row-count records, Spark/GX manifest and standalone-report success, all 43 comparison records successful, four successful zero-mismatch keyed records in each of the three families, empty DuckDB and Spark/Trino relation-error maps, the locked bundle/manifest/config hashes, four finished Trino queries with no failed root-stage tasks, and the 26-table/539,187-row executive mart. `run_batch_summary.json` records the exact locked backfill window, `parity_success=true`, executive table count `26`, total row count `539187`, and twelve captured artifacts. Spark master/worker/history and Trino coordinator/worker remained healthy with `OOMKilled=false`, `ExitCode=0`, and `RestartCount=0`. |
| Authoritative evidence and hashes | Preserve `tmp/section03-runtime/spark-strict-v4` as the authoritative local Topic 04 evidence root and do not overwrite it. The secret scan returned `1` with no matches; all 14 files were hashed. Primary SHA-256 values: `run_manifest.json` `dec061ea7e2ce94a2e921099249ee10c891f77a9350e5ac6befabf0af272c5bb`; `spark_job_manifest.json` `34ca3097c7cf8c7e63b4cd90a1c2ebbe0ae0ee01fb13f106adb0492f39457294`; `spark_table_row_counts.json` `c7f19b17c031ac805c317a81bc5a45d47dc2c6b16fca07f9413f6d12ffb3306c`; `dbt_parity_report.json` `36f3d9206837960f2e60ae7c11b845895af1eed8ddbc34737e65fe7b218fe40a`; `run_batch_summary.json` `efc4d016df914a111a1a8a58241b68134b5e1441bc6933225d5e9a9e32bac752`; `executive_mart_export_manifest.json` `c1c77e79461f0c617eed78b2b3cb2ff4ef34eb7c7c753728dcd1a7a48f09f868`; `pyspark_validation_report.json` `f7498a5d360999700b42f8934c8a82ebcff9f7011d9d2ea5ba1ce4d81f3d560d`; `gx/validation_results.json` `63874e8c3e3bcf4e026e8e30f29bb751a26f9cab9cab3e31bf2e975bcc804961`; `trino_gold_smoke_results.json` `5e546440ffe5711cb5d1a14c306d5a2d58a9729914ba71f015c137313fc69933`. Earlier roots remain historical: `spark` parity `b151ebe4f3139e219681bb51c5a2a96b0fa0e70437d647c49ffb6df04a86abb2`; `spark-strict-v2` `da084a957c81e7de12830a2afed3dcbd5df622b996c12af02f01c4dfcc6afcca`; content-valid but exit-unobserved `spark-strict-v3` `21200b0a1d02d1514f534b403fefcda83f0b2f90dd0583827dfc6f2debdd3864`. |
| Cleanup, restoration, and final verification | `rtk make down-batch` returned `0`; no project container or volume remains, the unrelated Docupedia container was untouched, and no Docker prune was used. Predecessor analytics state was restored exactly from the v4 backup: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, one dbt log, and no executive DuckDB. The backup remains retained for review. The post-cleanup focused gate returned `47 passed`; the fresh full suite returned `501 passed, 1 skipped` in 441.34 seconds. `git diff --check` passed; only the six expected tracked files are modified, all changes remain unstaged, branch/HEAD are unchanged, and `FC: no differences encountered` proves the final index snapshot is byte-identical to the pre-execution snapshot. |
| Evidence ownership and rubric disposition | No screenshot was created or required. This is authoritative local Compose/Spark/Iceberg/Trino machine evidence only. No Kubernetes command was run, so no kubeconfig/context applies, and no result is represented as GKE evidence. Topic 04 supplies complete supporting local evidence for `Sheet3!E34`; Topic 07 remains the sole primary owner and must combine the later Spark, Airflow, and DataHub captures for final promotion evidence. |
| Successor handoff | Topic 05 may proceed using the immutable strict-v4 root and its hashes. It must not reinterpret any local Compose output as Kubernetes/GKE evidence, overwrite Topic 04 roots, or mutate the retained v4 recovery backup before handoff review. Topic 04 has no remaining stop condition or implementation gate. |

### Strict-v3 application success with unobserved child exit - 2026-08-06

This entry is authoritative for the latest 2026-08-06 continuation and supersedes earlier Topic 04 entries where they conflict. The fresh strict-v3 root passes every application-content gate, but Topic 04 remains fail-closed because the execution wrapper timed out before it observed the still-running child process's terminal exit code.

| Field | Result |
|---|---|
| Status | **Partial.** `tmp/section03-runtime/spark-strict-v3` is the first fresh root whose own artifacts prove all 26 Gold relations, all seven ordered DP3 relations, PySpark and GX success, successful Trino queries, a 26-table executive mart, empty relation errors, all 43 aggregate/relation comparisons, and all twelve keyed comparisons across `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark` at zero mismatches. The execution tool nevertheless returned outer exit `124` after 1,804.1 seconds while the `uv`/Python child processes continued. The children later disappeared after publishing the final `run_batch_summary.json`, but Windows retained no direct process-exit audit record. Because `scripts/spark/run_batch.py` prints once after the summary is persisted, the artifacts cannot by themselves prove that the child exited `0` rather than encountering a terminal output-pipe error. The explicit strict-command exit-`0` gate is therefore the sole remaining blocker; Topic 05 remains blocked. |
| Identity and locked inputs | The session stayed on `feature/implement-edai2` at `2fbd85ec5899475bf1dd65386263ed2651dc993d`; the starting and final index contain the same 828 tracked entries. Locked SHA-256 values matched: Section 03 plan `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`; candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`. Manifest verification returned `section03 manifest: PASS (runtime pending)`. The predecessor DuckDB began and ended at SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`. No branch/worktree operation, stage, commit, push, install, Kubernetes, Kind, GCP, certificate, Spark resource, tolerance, rounding, or source-data change occurred. |
| Corrections retained | The unstaged correction set explicitly clears inherited upper- and lower-case HTTP/HTTPS/ALL proxy variables for Spark master, worker, and history plus Trino coordinator and worker, while supplying internal-only `NO_PROXY`/`no_proxy` names. Compose contract tests cover all five services. `_normalise_scalar` now canonicalizes only valid strings ending `Z` or ` UTC` to UTC ISO form; ordinary and invalid strings remain exact. Its regression test uses the actual generator/Trino timestamp encodings. The earlier `_run_command` captured-output re-emission contract and test are already present at this HEAD and remain green. No SQL, table inventory, generator, dbt, tolerance, or resource configuration was changed. |
| Preflight and runtime | Focused preflight returned `47 passed`. No project container or volume existed; the Spark image was present; host, Docker, and disk capacity gates passed; the v3 root and backup path were fresh. `rtk make up-batch` returned `0`; both init services exited `0` and all eight long-running services became healthy. The proxy-empty uploader landed all ten required batch snapshots and four required event topics; independent object checks returned `BRONZE_REQUIRED_INVENTORY=PASS`. Spark master/worker/history and Trino coordinator/worker each returned `INTERNAL_PROXY_PREFLIGHT=PASS`, and a real Bronze read returned `SPARK_S3A_BRONZE_PREFLIGHT=PASS 409549`. The two prior failed-root parity hashes remained unchanged: `spark` `b151ebe4f3139e219681bb51c5a2a96b0fa0e70437d647c49ffb6df04a86abb2` and `spark-strict-v2` `da084a957c81e7de12830a2afed3dcbd5df622b996c12af02f01c4dfcc6afcca`. |
| Strict-v3 content result | The exact locked window was `2026-03-03T23:59:00Z` through `2026-05-01T23:59:00Z` in `backfill` mode. `SECTION03_SPARK_PARITY=PASS` asserted the exact 26-table inventory, the final seven entries equal `DP3_GOLD_TABLES`, 26 Gold row-count records, Spark/GX manifest and standalone-report success, all 43 comparison records successful, four successful zero-mismatch keyed records in each of the three families, empty DuckDB and Spark/Trino relation-error maps, the locked manifest/config hashes, four finished Trino queries with no failed root-stage tasks, and the 26-table/539,187-row executive mart. `run_batch_summary.json` is written only after Spark, dbt, parity, Trino smoke, executive export, and evidence capture return successfully; it records `parity_success=true`, executive table count `26`, and twelve captured artifacts. Spark master/worker and Trino coordinator/worker remained healthy with `OOMKilled=false`, `ExitCode=0`, and `RestartCount=0`. |
| Evidence and hashes | The secret scan returned `1` with no matches. The 14-file v3 inventory was hashed. Primary SHA-256 values: `run_manifest.json` `9a3496f240a316d69efbfec3cbe9c9df3ccfe405623bbc71edebc0cbb6cd5cb0`; `spark_job_manifest.json` `34ca3097c7cf8c7e63b4cd90a1c2ebbe0ae0ee01fb13f106adb0492f39457294`; `spark_table_row_counts.json` `c7f19b17c031ac805c317a81bc5a45d47dc2c6b16fca07f9413f6d12ffb3306c`; `dbt_parity_report.json` `21200b0a1d02d1514f534b403fefcda83f0b2f90dd0583827dfc6f2debdd3864`; `run_batch_summary.json` `b30273202c8c0fe2456bac848ea7fcfd0cdf4efe6fea4b730660ec3abe59fc47`; `executive_mart_export_manifest.json` `a998967f046a28bf30841697a09ce372375fa14630cfc8220f847297d2760349`; `pyspark_validation_report.json` `f7498a5d360999700b42f8934c8a82ebcff9f7011d9d2ea5ba1ce4d81f3d560d`; `gx/validation_results.json` `63874e8c3e3bcf4e026e8e30f29bb751a26f9cab9cab3e31bf2e975bcc804961`; `trino_gold_smoke_results.json` `92e03a76ea021e75f4dc011c642d408da25d013ede5cd6098bc213769fc862ba`. Preserve v3 as content-valid but not yet promotable evidence; never overwrite it. |
| Cleanup, restoration, and verification | `rtk make down-batch` returned `0`; no project container or volume remains, unrelated Docupedia containers were untouched, and no Docker prune was used. Predecessor analytics state was restored from `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-v3-20260806`: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, one dbt log, and no executive DuckDB. The backup remains retained for review. The post-cleanup focused gate returned `47 passed`; the fresh full suite returned `501 passed, 1 skipped` in 468.10 seconds. `git diff --check` passed; only the six expected tracked files are modified, all changes remain unstaged, branch/HEAD are unchanged, and `FC: no differences encountered` proves the final index snapshot is byte-identical to the pre-execution snapshot. |
| Locality and rubric disposition | No screenshot was created or required. This is local Compose/runtime evidence only. No Kubernetes command was run, so no kubeconfig/context applies; no result is represented as GKE evidence. Topic 04 supplies supporting local evidence for `Sheet3!E34`; Topic 07 remains sole primary owner. Because direct strict-command exit `0` was not captured, no promotion credit is claimed and Topic 05 remains blocked. |
| Exact completion path | Obtain explicit authorization for one fresh observation-only strict continuation because the original one-attempt budget is consumed. Preserve all three existing roots and use a new root such as `tmp/section03-runtime/spark-strict-v4`. Repeat the locked identity, ownership, capacity, backup, Bronze inventory, five-service proxy, and real S3A gates without changing code or runtime resources. Run the same locked-window command once with an execution-tool timeout of at least 3,600,000 ms, preferably 5,400,000 ms, so the direct `rtk uv run python scripts/spark/run_batch.py ...` invocation returns an observed exit code. Require observed exit `0`, then rerun the complete v3 content assertion against v4, secret scan and hashes, release only the owned stack, restore DuckDB/dbt state, run focused and full tests, and prove branch/index identity. Only then add a Complete entry and unblock Topic 05. Any nonzero/timeout result or any changed content gate leaves Topic 04 Partial; do not retry, tune resources, relax tolerance, alter certificates/proxies, overwrite a prior root, or expand into Kubernetes/GCP. |

### UTC timestamp-normalization strict continuation - 2026-08-06

This entry is authoritative for the latest 2026-08-06 continuation and supersedes earlier Topic 04 entries where they conflict. Spark, Trino, all 26 Gold relations, aggregate parity, and three of four keyed relations now agree; the strict root remains fail-closed because its pre-correction report compared equivalent UTC timestamp encodings as different strings.

| Field | Result |
|---|---|
| Status | **Partial.** The fresh strict-v2 attempt passed every startup, Bronze, S3A, Trino access, 26-table, seven-DP3, PySpark/GX, aggregate, relation-error, label, feature-health, and drift-alert gate. It returned `1` at `RuntimeError: dbt parity checks failed.` because both Spark-involving `ml_customer_purchase_training` comparisons treated Trino UTC timestamp strings as different from equivalent generator/dbt UTC values. The comparator defect is now corrected test-first, and a read-only comparison of the actual runtime rows proves all twelve keyed comparisons at zero. The single strict attempt was not rerun, its `success=false` report is retained unchanged, no downstream Trino smoke/mart/summary artifact exists, and Topic 05 remains blocked. |
| Identity and locked inputs | The session stayed on `feature/implement-edai2` at `2fbd85ec5899475bf1dd65386263ed2651dc993d`; the starting index contained 828 tracked entries. Locked SHA-256 values matched: Section 03 plan `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`; candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`. Manifest verification returned `section03 manifest: PASS (runtime pending)`. The prior failed-root parity hash remained `b151ebe4f3139e219681bb51c5a2a96b0fa0e70437d647c49ffb6df04a86abb2`; `tmp/section03-runtime/spark-strict-v2` and the fresh backup path were absent before execution. No branch/worktree operation, stage, commit, push, install, Kubernetes, Kind, GCP, certificate, resource, tolerance, rounding, or source-data change occurred. |
| Preflight and runtime | Focused preflight returned `46 passed`; rendered Compose, image, memory, disk, project-ownership, and raw-input gates passed. `rtk make up-batch` returned `0`; both init services exited `0` and all eight long-running services became healthy. The explicit proxy-empty uploader landed all ten required batch snapshots and all four required event topics at `2026-05-01`; independent checks returned `BRONZE_REQUIRED_INVENTORY=PASS`. Spark master/worker/history and Trino coordinator/worker all returned `INTERNAL_PROXY_PREFLIGHT=PASS`. The actual Bronze customer object returned `SPARK_S3A_BRONZE_PREFLIGHT=PASS 409549`. |
| Strict-v2 result | The exact locked-window command used the initially absent root `tmp/section03-runtime/spark-strict-v2` and returned `1` after 205 seconds. The root contains 26 required Gold row counts and all seven DP3 relations; PySpark and GX validation flags/reports are true. All 26 row-count comparisons and all five financial aggregate comparisons passed. `relation_errors.duckdb` and `relation_errors.spark_trino` are both empty. `generator_vs_dbt` passed all four keyed relations; labels, feature health, and drift alerts also passed in both Spark-involving families. Only `ml_customer_purchase_training` failed: `11996` field mismatches in each of `generator_vs_spark` and `dbt_vs_spark`, with zero missing or extra keys. All Spark and Trino containers remained healthy, `OOMKilled=false`, `ExitCode=0`, `RestartCount=0`. |
| Root-cause evidence | For key `CUS-BDG-00000004`, every non-timestamp field agreed. Generator values were `event_timestamp=2026-04-24T23:59:00Z` and `created=2026-04-24T23:59:00Z`; dbt returned the equivalent Python datetimes; Trino returned `2026-04-24 23:59:00.000 UTC`. `_normalise_scalar` canonicalized Python dates/datetimes but returned all strings unchanged, so equivalent Trino timestamps caused one field-mismatched row for every training key. This exact two-column representation difference accounts for all `11996` failures; it is not a SQL, key, label, float, tolerance, missing-row, proxy, metadata-access, or data-content mismatch. |
| Test-driven correction | `tests/unit/test_spark_batch_runtime.py` adds `test_keyed_training_comparison_normalises_trino_utc_timestamp_strings`, exercising `compare_keyed_rows` with the exact generator and Trino encodings. It first failed with `mismatch_count=1`. `src/vina_bim_shop/lakehouse/spark/parity.py` then received the minimal correction: parse only valid string timestamps ending in `Z` or ` UTC`, canonicalize them to UTC ISO form, and leave ordinary or invalid strings exact. The isolated node returned `1 passed`; the post-change focused gate returned `47 passed`. Float rounding remains twelve decimals with absolute tolerance `1e-9`; labels, keys, non-timestamp strings, dates, and booleans retain their existing semantics. |
| Live post-fix parity proof | Without modifying the strict report or rerunning the pipeline, the corrected comparator read the actual candidate CSV rows, current dbt DuckDB rows, and current Trino/Iceberg rows. `LIVE_THREE_WAY_PARITY_DIAGNOSTIC=PASS` proved zero mismatches for labels, training, feature health, and drift alerts in each of `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark`. This proves the correction against all runtime rows but does not retroactively promote the pre-correction strict report. |
| Retained invalid diagnostic evidence | `TOPIC04_TIMESTAMP_ONLY_STRICT_FAILURE=CONFIRMED` asserted the exact 26-table/seven-DP3/validation and timestamp-only failure pattern. The secret scan returned `1` with no matches. SHA-256: `spark_job_manifest.json` `34ca3097c7cf8c7e63b4cd90a1c2ebbe0ae0ee01fb13f106adb0492f39457294`; `spark_table_row_counts.json` `c7f19b17c031ac805c317a81bc5a45d47dc2c6b16fca07f9413f6d12ffb3306c`; `pyspark_validation_report.json` `f7498a5d360999700b42f8934c8a82ebcff9f7011d9d2ea5ba1ce4d81f3d560d`; `gx/validation_results.json` `63874e8c3e3bcf4e026e8e30f29bb751a26f9cab9cab3e31bf2e975bcc804961`; `dbt_parity_report.json` `da084a957c81e7de12830a2afed3dcbd5df622b996c12af02f01c4dfcc6afcca`; `dbt_parity_report.md` `1374d95d88dcf440a012c0e39ba5ba5863735a6eafe5b0cc8d9afe463648ec6a`. `tmp/section03-runtime/spark-strict-v2` is invalid diagnostic material and must not be overwritten or used as successor evidence. No `run_batch_summary.json` exists because parity raised before Trino smoke, executive export, capture, and summary persistence. |
| Cleanup, restoration, and verification | `rtk make down-batch` returned `0`; no project container or volume remains, unrelated Docupedia containers were not stopped, and no Docker prune was used. Predecessor analytics state was restored from `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-final-20260806`: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, one dbt log file, and no executive DuckDB. The post-cleanup focused gate returned `47 passed`; the fresh full suite returned `501 passed, 1 skipped` in 499.09 seconds. `git diff --check` passed; only the six expected tracked files are modified, all changes remain unstaged, branch/HEAD are unchanged, and the final index snapshot is byte-identical to the pre-execution snapshot. The backup remains retained for review. |
| Locality and rubric disposition | No screenshot was created or required. This is local Compose/runtime evidence only. No Kubernetes command was run, so no kubeconfig/context applies; no result is represented as GKE evidence. Topic 04 remains supporting evidence only for `Sheet3!E34`; Topic 07 remains sole primary owner. Because the retained strict report is false and no downstream summary/mart artifact exists, no E34 promotion credit is claimed. |
| Successor completion gate | In a fresh authorized continuation, preserve the Spark/Trino proxy tests and UTC normalization, start a new owned stack, land/reassert all ten batch and four event inputs, and pass the same five service and S3A preflights. Use a new root such as `tmp/section03-runtime/spark-strict-v3`; never overwrite either failed root. Run exactly one strict locked-window attempt. Mark Complete only when the strict command exits `0`, its own parity report records four zero-mismatch comparisons in all three families, all relation/aggregate/26-table/seven-DP3/PySpark/GX/Trino/executive-mart gates pass, secret scan is clean, `run_batch_summary.json` records `parity_success=true` and executive table count `26`, cleanup/restoration succeeds, full tests pass, and branch/index remain unchanged. Any failed gate leaves Topic 04 Partial and Topic 05 blocked. |

### Trino proxy-isolation and full Spark diagnostic continuation - 2026-08-06

This entry is authoritative for the latest 2026-08-06 continuation and supersedes the earlier proxy-isolation entries where they conflict. Spark now completes all 26 Gold outputs and both local validation layers; the strict root remains fail-closed because its parity report was generated before the newly isolated Trino proxy correction.

| Field | Result |
|---|---|
| Status | **Partial.** The verified Bronze preflight allowed the single strict attempt to complete Spark/Iceberg generation, all 26 Gold row-count records, all seven ordered DP3 relations, PySpark validation, Great Expectations validation, and dbt. The run then failed at the Trino-backed parity boundary with `RuntimeError: dbt parity checks failed.` The causal Trino proxy defect is now corrected, test-covered, and live-verified against the produced Iceberg data, but the one strict attempt was not retried and its original `success=false` parity artifact is retained unchanged. No Trino smoke, executive mart, run summary, or valid strict root exists yet; Topic 05 remains blocked. |
| Identity, inputs, and ownership | The session stayed on `feature/implement-edai2` at `2fbd85ec5899475bf1dd65386263ed2651dc993d`; the 828-entry index snapshot was preserved. Locked SHA-256 values matched: Section 03 plan `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`; candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`. Manifest verification returned `section03 manifest: PASS (runtime pending)`. There were no pre-existing project containers/volumes; memory, disk, image, and fresh-root gates passed. No branch/worktree operation, stage, commit, push, install, Kubernetes, Kind, GCP, certificate, Spark resource, tolerance, or normalization change occurred. |
| Bronze preflight | The repository raw tree contained all ten `BRONZE_BATCH_DATASETS` and all four `BRONZE_EVENT_TOPICS`. The first temporary uploader container reproduced HTTP `502` because it inherited `http://host.docker.internal:3128` and its `NO_PROXY` omitted `minio`; no objects were written by that attempt. An explicit proxy-empty/internal-no-proxy `mc ready` probe passed, then the existing combined uploader landed all ten required Parquet snapshots and all four required event JSONL objects at `2026-05-01` plus the optional dead-letter object. Independent `mc stat` checks returned `BRONZE_REQUIRED_INVENTORY=PASS`. All Spark services passed proxy-environment and direct-MinIO checks; a real-object S3A probe returned `SPARK_S3A_BRONZE_PREFLIGHT=PASS 409549`. |
| Strict Spark and validation result | The exact locked-window command against initially absent `tmp/section03-runtime/spark` returned `1` after 982.7 seconds. `spark_job_manifest.json` records the exact 26 required Gold relations and 26 Gold row-count entries; all seven DP3 relations are present. Representative counts are `ml_customer_label=11996`, `ml_customer_purchase_training=11996`, `agg_feature_health_daily=22`, and `feature_drift_alerts=0`. Both `pyspark_validation_success` and `gx_validation_success` are `true`, and their standalone reports also record success. Spark master, worker, and history server remained healthy, `OOMKilled=false`, `ExitCode=0`, `RestartCount=0`; this attempt had no S3A 502, worker OOM, heartbeat loss, or executor-startup failure. |
| Parity result and root cause | `generator_vs_dbt` passed all four keyed comparisons with zero mismatches. Before the Trino fix, `relation_errors.spark_trino` recorded `Error accessing metadata file` for all 26 Gold relations and four metric checks. Consequently, `generator_vs_spark` and `dbt_vs_spark` each reported missing-key counts of `11996` for labels, `11996` for training, and `22` for feature health; the zero-row drift-alert comparison passed. Trino coordinator and worker both inherited the host proxy and omitted `minio` from both no-proxy variants; direct coordinator-to-MinIO curl returned HTTP `502`. Clearing the proxy variables for a diagnostic command made direct MinIO access pass on both services, proving the boundary. |
| Test-driven Trino correction | `tests/unit/test_lakehouse_compose_profile.py` added `test_trino_services_bypass_inherited_proxy_for_internal_lakehouse`; it first failed with `KeyError: 'HTTP_PROXY'`. `compose/lakehouse.yml` then received the minimal symmetric correction for `trino` and `trino-worker`: clear all six uppercase/lowercase proxy variables and set both no-proxy variants to `minio,hive-metastore,trino,trino-worker,localhost,127.0.0.1,::1`. The isolated test then returned `1 passed`; rendered Compose validation passed; the combined lakehouse/Spark/optional-DuckDB gate returned `46 passed`. No Trino catalog, SQL, image, credentials, memory, or worker-count setting changed. |
| Live post-fix proof | Only the session-owned Trino coordinator and worker were recreated; the strict pipeline was not rerun. Both services became healthy, passed exact proxy/no-proxy assertions, and returned `TRINO_PROXY_AND_MINIO=PASS`. A Trino Iceberg metadata query returned the expected `11996` label rows. A real data scan returned `['CUS-BDG-00000004', 0]`, processed `11996` rows and `41231` bytes, and printed `TRINO_ICEBERG_DATA_READ=PASS`. This proves the correction at both metadata and data-read boundaries but does not retroactively validate or promote the pre-fix parity report. |
| Retained invalid diagnostic evidence | `tmp/section03-runtime/spark` is retained as explicitly invalid diagnostic material and must not be reused as a successor root. `TOPIC04_PARTIAL_DIAGNOSTIC=CONFIRMED` asserted the 26-table/seven-DP3/validation state, zero generator/dbt mismatches, and exact Trino-caused missing-key pattern. The secret scan returned `1` with no matches. SHA-256: `spark_job_manifest.json` `34ca3097c7cf8c7e63b4cd90a1c2ebbe0ae0ee01fb13f106adb0492f39457294`; `spark_table_row_counts.json` `c7f19b17c031ac805c317a81bc5a45d47dc2c6b16fca07f9413f6d12ffb3306c`; `pyspark_validation_report.json` `f7498a5d360999700b42f8934c8a82ebcff9f7011d9d2ea5ba1ce4d81f3d560d`; `gx/validation_results.json` `63874e8c3e3bcf4e026e8e30f29bb751a26f9cab9cab3e31bf2e975bcc804961`; `dbt_parity_report.json` `b151ebe4f3139e219681bb51c5a2a96b0fa0e70437d647c49ffb6df04a86abb2`; `dbt_parity_report.md` `796a6940e216a4f5dc64871da155c2767d4a7aa189294bbdcccb7ed2b846dd46`. No `run_batch_summary.json` exists because parity raised before Trino smoke, executive export, evidence capture, and summary persistence. |
| Cleanup, restoration, and tests | `rtk make down-batch` returned `0`; no project container or volume remains, unrelated Docupedia containers were not stopped, and no Docker prune was used. Predecessor analytics state was restored from `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-completion-20260806`: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, one dbt log file, and no executive DuckDB. The post-cleanup focused gate returned `46 passed`; the fresh full suite returned `500 passed, 1 skipped` in 556.08 seconds. The backup remains retained for review. |
| Locality and rubric disposition | No screenshot was created or required. This is local Compose evidence only. No Kubernetes command was run, so no kubeconfig/context applies; no result is described as GKE evidence. Topic 04 continues to provide supporting evidence only for `Sheet3!E34`; Topic 07 remains sole primary owner. Because the retained parity report is false and the downstream Trino/mart/summary gates did not run, no E34 promotion credit is claimed. |
| Successor completion gate | In a fresh authorized continuation, keep both tested Spark and Trino proxy contracts, start a new owned stack, land all ten batch and four required event inputs using the explicit proxy-empty/internal-no-proxy uploader invocation, reassert the complete Bronze inventory, and pass both S3A and Trino-to-MinIO preflights. Use a new evidence root such as `tmp/section03-runtime/spark-strict-v2`; never overwrite the retained failed root. Run exactly one strict locked-window attempt. Mark Complete only when it exits `0` and proves 26 Gold relations, seven ordered DP3 relations, successful PySpark/GX/Trino/executive-mart checks, a clean secret scan, and four zero-mismatch keyed comparisons in each of `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark`, followed by cleanup/restoration, full tests, unchanged branch/index, and a `run_batch_summary.json` with `parity_success=true` and executive table count `26`. Any failed preflight, strict error, secret-bearing artifact, cleanup/restoration failure, or unexpected tracked/index mutation leaves Topic 04 Partial and Topic 05 blocked. |

### Spark proxy-isolation continuation - 2026-08-06

This entry is authoritative for the latest 2026-08-06 continuation and supersedes the earlier diagnostic-first entry where they conflict. The proxy fault is resolved; the strict parity gate remains fail-closed on a newly isolated Bronze-input prerequisite.

| Field | Result |
|---|---|
| Status | **Partial.** The Spark services now deterministically bypass inherited host proxy variables for Compose-internal traffic, the direct S3A canary passed, and the strict application initialized `SparkContext`, registered an executor, and wrote its event log to `s3a://checkpoints/spark-events`. The single permitted strict attempt then stopped before Gold processing because the fresh MinIO Bronze bucket contained no required batch inputs. No retry was performed, no evidence root or three-way parity pass is claimed, and Topic 05 remains blocked. |
| Assumptions and locked inputs | The session stayed on `feature/implement-edai2` at `2fbd85ec5899475bf1dd65386263ed2651dc993d`. The starting worktree/index were clean with 828 tracked index entries. Locked SHA-256 values matched: Section 03 plan `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`; candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`. Manifest verification returned `section03 manifest: PASS (runtime pending)`. There were no pre-existing project containers/volumes and the strict root was absent. The pinned Spark image remained `sha256:354594711c9a55a8718c0f6595ef039defbb9aa34ac82eb7c29285d3b8707fcb`; host/Docker memory and disk gates passed. No branch/worktree operation, staging, commit, push, install, Kubernetes, Kind, GCP, certificate, resource, tolerance, or proxy-daemon change occurred. |
| Affected files and interfaces | `compose/batch.spark.yml` explicitly clears `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, and their lowercase variants for `spark-master`, `spark-worker`, and `spark-history-server`, and supplies identical uppercase/lowercase internal `NO_PROXY` values for `minio`, `hive-metastore`, all three Spark service names, `localhost`, `127.0.0.1`, and `::1`. `tests/unit/test_spark_batch_runtime.py` adds a rendered-Compose contract covering all three services. Public Python/Spark interfaces, Spark memory/cores, certificates, proxy service settings, SQL, parity rules, and the pinned image are unchanged. This Completion Record is the only other tracked edit. |
| Test-first and repository verification | The new Compose contract first failed with `KeyError: 'HTTP_PROXY'`, proving the inherited-proxy gap. After the minimal Compose edit, the isolated node returned `1 passed`; the focused gate returned `38 passed` before runtime and again after cleanup. `docker compose ... config --quiet` and `git diff --check` passed. The final full `rtk uv run pytest -q` returned `0` with `499 passed, 1 skipped` in 460.86 seconds. |
| Live proxy/S3A proof | `rtk make up-batch` returned `0`; both init services exited `0`, and all eight long-running batch services became healthy. In each Spark container, all six proxy variables were empty and both no-proxy variants contained the locked internal host set. Direct MinIO readiness probes from master, worker, and history server returned `0`. A corrected local PySpark probe returned `SPARK_S3A_PREFLIGHT=PASS`. The history server remained healthy with no `AWSStatus500Exception`, HTTP `502`, or proxy match. These checks confirm that the proposed `127.0.0.1:3128` proxy can remain a host setting only when Compose-internal Spark traffic is explicitly isolated as implemented; `127.0.0.1` itself is not a usable route from a container to a host proxy. |
| Strict result and causal exception | The exact locked-window strict command returned `1` after about 13.4 seconds. Spark logged events to `s3a://checkpoints/spark-events/eventlog_v2_app-20260806104334-0000`, registered executor `app-20260806104334-0000/0`, and then `_create_raw_views` / `_read_required_parquet` raised `RuntimeError: Missing Bronze batch inputs for dataset 'customers' and dates ['2026-03-03', ..., '2026-05-01']`. The master, worker, and history server remained `running`, `OOMKilled=false`, `ExitCode=0`, `RestartCount=0`; the worker killed the executor only after the application unregistered. The history server subsequently parsed the S3A event log. This rules out the previous proxy/502 boundary, worker OOM, heartbeat loss, and autonomous executor failure for this attempt. |
| Bronze prerequisite diagnosis | The fresh MinIO `bronze` bucket was present but empty. `make up-batch` creates buckets; it does not land repository data, and `run_batch.py` expects the required Bronze objects to exist. `scripts/lakehouse/land_bronze_batch.py` is the official deterministic batch-snapshot uploader and excludes `kafka_topics`. Required event landing exists in the coursework orchestration path, while `scripts/qa/upload_bronze.sh` is a manual combined helper. A completion attempt must therefore seed and verify both all required batch datasets and every `BRONZE_EVENT_TOPICS` object for a date accepted by the locked window before consuming its one strict execution. |
| Cleanup and recovery | `rtk make down-batch` returned `0`; no project container or volume remains and unrelated Docupedia containers were not stopped. No Docker prune was used. The predecessor-owned analytics state was restored from `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-proxy-fix-20260806`: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 dbt target files, one dbt log file, and no executive DuckDB. `tmp/section03-runtime/spark` remains absent, so no partial output can be mistaken for valid evidence. The backup is retained for review. |
| Evidence, locality, and rubric disposition | Runtime output and causal logs are retained in this Codex task transcript; no valid strict evidence artifact or screenshot was created. This is local Compose evidence only. No Kubernetes command was run, so no kubeconfig/context applies, and no result is represented as GKE evidence. Topic 04 continues to supply supporting local evidence only for `Sheet3!E34`; Topic 07 remains sole primary owner and no promotion credit is claimed. |
| Successor plan and stop condition | Do not retry this failed root or relax the parity contract. In a fresh authorized session: preserve the proxy Compose contract; start a fresh batch stack; deterministically upload all ten required batch datasets and all required event topics at an in-window partition date; list/assert the complete Bronze object inventory and run the direct S3A canary; then run exactly one fresh strict root. Mark Complete only after 26 Gold relations, seven ordered DP3 relations, Spark/GX/Trino/executive-mart success, a clean secret scan, and four zero-mismatch keyed comparisons in each of `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark`, followed by cleanup/restoration, full tests, and unchanged branch/index gates. Any incomplete Bronze inventory, strict failure, secret-bearing artifact, cleanup/restoration failure, or unexpected tracked/index mutation leaves Topic 04 Partial and Topic 05 blocked. |

### Diagnostic-first execution update - 2026-08-06

This entry is authoritative for the 2026-08-06 execution and supersedes older Topic 04 runtime diagnostics where they conflict. The strict local gate remains fail-closed.

| Field | Result |
|---|---|
| Status | **Partial.** The approved diagnostic correction and all repository tests pass, but the single permitted strict Spark attempt failed while initializing `SparkContext`, before any Gold relation or evidence root was created. No retry or runtime tuning was performed, no three-way parity pass is claimed, and Topic 05 remains blocked. |
| Assumptions verified | The session stayed on `feature/implement-edai2` at `3063490b4f894347a3ceedbb6b4150f2b1783b12`. The starting worktree/index were clean with 828 tracked index entries. There were no project containers or volumes and `tmp/section03-runtime/spark` was absent. Capacity passed with 33,835,024 KiB free host RAM, 33,410,437,120 bytes assigned to Docker, and 171,693,391,872 bytes free on C:. The pinned Spark image remained `sha256:354594711c9a55a8718c0f6595ef039defbb9aa34ac82eb7c29285d3b8707fcb`. No branch/worktree operation, stage, commit, push, install, Kubernetes, Kind, GCP, certificate, proxy, tolerance, or resource-setting change occurred. |
| Locked inputs | Section 03 plan SHA-256 `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; EDAI2 plan `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`. Candidate manifest `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`, bundle `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`, and config `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97` matched. Manifest verification returned `section03 manifest: PASS (runtime pending)`. |
| Affected files and interfaces | `tests/unit/test_spark_batch_runtime.py` adds `test_run_command_reemits_captured_output_before_reraising`. `src/vina_bim_shop/lakehouse/spark/runner.py` keeps `_run_command(command: list[str]) -> subprocess.CompletedProcess[str]` and its successful subprocess invocation unchanged; on `CalledProcessError` it re-emits the captured stdout/stderr to their original streams, flushes both, and re-raises the identical exception. This Completion Record is the only other tracked modification. `sql.py`, `constants.py`, `job.py`, `parity.py`, `scripts/spark/run_batch.py`, and `test_optional_duckdb_imports.py` were not modified. |
| Test-first results | The new node first returned `1` with the expected assertion `captured.out == ''` instead of `captured stdout\n`. After the minimal exception-path change, the same node returned `0` with `1 passed`. The focused gate returned `0` with `37 passed` before runtime and again after cleanup. The fresh full `rtk uv run pytest -q` returned `0` with `498 passed, 1 skipped` in 500.77 seconds. |
| Runtime result and causal exception | `rtk make up-batch` returned `0`; both init services exited `0`, and the eight long-running services reached healthy state. The one strict command using the locked window and `tmp/section03-runtime/spark` returned `1` after 295.6 seconds. `build_spark_session().getOrCreate()` failed with `org.apache.hadoop.fs.s3a.AWSStatus500Exception` while running `getFileStatus` on `s3a://checkpoints/spark-events`; the nested AWS SDK exception reported HTTP `502` after five attempts. `SparkContext` therefore never initialized, the Spark history server independently exited `1` on the same S3A check, and no Gold/DP3/GX/Trino/mart/parity component completed. MinIO remained healthy and its service log contained no corresponding request/error; this evidence identifies the failing S3A event-log boundary but does not prove the origin of the `502`. |
| Failure evidence | The worker and master remained `running`, `OOMKilled=false`, `ExitCode=0`, `RestartCount=0`. The worker killed executor `app-20260806064619-0000/0` only after the application unregistered; there is no worker OOM, heartbeat loss, or autonomous executor failure. Retained under `C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-exec-20260806`: `spark-master-failure.log` SHA-256 `423acc75253b814f38dfadd22cf6d0a4a2bb4d38a468d69d6ac7bebde0fb4b8f`; `spark-worker-failure.log` `9e233125ee53c75d02dd346f5223487ace1461148bf551d83d09e860cafe69a8`; `spark-history-server-failure.log` `a1396708dd69f493d21e73efe2dea177e709af0c051204c166b2309dda3ffc51`; `minio-failure.log` `cc4b3606c13fd3dd6dbe674acd916aa309d0e36d4ca61ea6ce2fde812aa5afcd`; `compose-ps-failure.txt` `0e98647734c53baa333aa60451601cb50630aa0f6a64446c9e36e5cb417dc709`. The diagnostic secret scan returned `1` with no matches. `tmp/section03-runtime/spark` remains absent, so no partial output can be mistaken for valid evidence. |
| Cleanup and recovery | `rtk make down-batch` returned `0`. No project container or volume remains, and the pre-existing unrelated Docupedia container `003da36b59eb` (`zen_fermi`) remained running. No Docker prune was used. The canonical DuckDB and dbt target/log state were restored from the retained backup: DuckDB SHA-256 `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`, 301 target files, one log file, and no `data/gold/vina_bim_shop_executive.duckdb`. |
| Final repository acceptance | `rtk git diff --check` returned `0`. The branch and HEAD remained `feature/implement-edai2` and `3063490b4f894347a3ceedbb6b4150f2b1783b12`. `rtk git diff --name-only` listed exactly `src/vina_bim_shop/lakehouse/spark/runner.py`, `tests/unit/test_spark_batch_runtime.py`, and this Completion Record. The final 828-entry index snapshot compared byte-for-byte with the pre-edit snapshot: `FC: no differences encountered`. All changes remain unstaged. |
| Screenshot and locality | No screenshot was created or required. This is local diagnostic evidence only. No Kubernetes command was run, so no kubeconfig/context applies; no local result is described as GKE evidence. |
| Rubric disposition | Topic 04 still contributes only supporting local evidence for `Sheet3!E34`; candidate/pending state earns no promotion credit. Topic 07 remains the sole primary owner. The required 26-table, seven-DP3, validation, secret-scan, and three-family zero-mismatch runtime gates were not reached. |
| Stop condition and successor | The single permitted strict attempt is exhausted. Do not retry, tune resources, change proxy/certificate settings, relax `1e-9`, change 12-decimal normalization, regenerate the candidate, or reuse stale reports. Topic 05 remains blocked. A later explicitly authorized session must first resolve the S3A `502` boundary, then run one fresh strict root and require all 26 Gold relations, all seven ordered DP3 relations, successful Spark/GX/Trino/mart checks, and zero mismatches in all four keyed comparisons across `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark` before marking Complete. |

### Remediation continuation update — 2026-08-04

This entry is authoritative for the current remediation continuation and supersedes earlier Topic 04 entries where they conflict. The strict local Spark gate remains fail-closed.

| Field | Result |
|---|---|
| Status | **Partial.** The one generator semantic mismatch is repaired, the medium seed-42 candidate is reissued, default and `--all-gold` dbt contracts pass, and the complete 26-relation Gold inventory has zero generator/dbt mismatches. The two allowed strict Spark attempts both ended with executor exit 143 before publishing the new evidence root, so no three-way parity pass is claimed. |
| Assumptions verified | The checkout remained on `feature/implement-edai2` at the existing `6b67259` lineage. The pre-edit index listing was preserved and the final listing was compared byte-for-byte. No branch/worktree switch, stage/unstage, commit, push, PR, Kubernetes, Kind, GCP, cloud-runtime, certificate, parity-tolerance, or screenshot change was made. The Bosch CA and MinIO fixes were not modified. |
| Locked hashes | `tmp/edai2-plan/03_data_generator_improvement.md` = `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`; `tmp/edai2-plan/04.2_llm_design.md` = `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`; `tmp/rubic-check/Coursework Tracking (Public).xlsx` = `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`. |
| Candidate identity | `evidence/03_data_generator_improvement/section03_candidate_manifest.json` SHA-256 `9b1fc0b5d9cea1186e173e263e04cd363f65f791110d02345a99dbbe99ae5475`; bundle ID `0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d`; `configs/generator/base.yaml` SHA-256 `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`. The manifest records `previous_bundle_id: null`; the prior bundle `1b4123b312da3d1aca70c2dc4f44cf68db248f3b43aad830e8519ca7bbab7fd3` remains present/recoverable and was not overwritten. |
| Affected files and interfaces | Topic 03 follow-up changed `src/vina_bim_shop/generators/labels.py` to select only the latest UTC hour after the existing 60-minute, event-time, and created-time filters; `tests/unit/test_section03_drift.py` added the behavioral regression; `tests/unit/test_section03_dbt_contract.py` and `tests/unit/test_spark_batch_runtime.py` retain/strengthen latest-hour and cutoff-predicate contracts. Candidate regeneration changed `evidence/01_data_generator/run_manifest.json`, `evidence/03_data_generator_improvement/section03_candidate_manifest.json`, added the new bundle directory under `evidence/03_data_generator_improvement/runs/0e889d1e57425e070a87f406e220f758d703326eac67ea1d5f9f58fa2916bb9d/`, and added `evidence/03_data_generator_improvement/section03.lock`. The Topic 03 Completion Record was updated with the new candidate identity. No Spark resource, CA, dbt SQL, Kubernetes, or GCP file was changed in this continuation. |
| Test-first and upstream results | The new latest-hour test first failed with `1` (`add_to_cart` incorrectly counted), then passed with `1 passed` after the minimal generator change. The combined focused generator/dbt/Spark contract command returned `0` with `91 passed`. Candidate generation returned `0`; manifest verification returned `0` (`section03 manifest: PASS (runtime pending)`). The default dbt wrapper returned `0`: `PASS=88 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=88`. The explicit `--all-gold` wrapper returned `0`: `PASS=133 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=133`. Read-only inventory returned `26/26`, with no missing, unexpected, or duplicate relations. Read-only keyed generator/dbt comparison returned zero missing, extra, duplicate, and field mismatches for `ml_customer_label`, `ml_customer_purchase_training`, `agg_feature_health_daily`, and `feature_drift_alerts`. |
| Runtime commands and results | `rtk make up-batch` returned `0`; `minio-init` exited `0` and the local Compose services became healthy. The prescribed strict command using `--evidence-root tmp/section03-runtime/topic04-spark-gold-20260804-r3` returned `1` after about 263.3 seconds. The one final bounded retry, run as `rtk proxy uv run python scripts/spark/run_batch.py --start-ts 2026-03-03T23:59:00Z --end-ts 2026-05-01T23:59:00Z --mode backfill --evidence-root tmp/section03-runtime/topic04-spark-gold-20260804-r3 --generator-config configs/generator/base.yaml --generator-scale medium --section03-manifest evidence/03_data_generator_improvement/section03_candidate_manifest.json`, returned `1` after about 319.9 seconds. Both attempts caused Spark executor exit `143`; the worker remained `running`, `OOMKilled=false`, `ExitCode=0`, `RestartCount=0`, and the logs contain no OOM or heartbeat error. The r3 evidence root is absent, so no Spark evidence was fabricated. No resource change was justified by the diagnostics. |
| Retained failure evidence | `C:\Users\oou1hc\AppData\Local\Temp\edai2-remediation-r2-20260804\spark-r3-worker-inspect.txt` SHA-256 `4598e14734d937dcdf87faafbd43289783fafaaf76d8ecde8495bef1c9293a91`; `spark-r3-worker.log` SHA-256 `554724a79406e8b8e57c54ef91ebc717aec939caa0fec6bfc93438cd9a5517ee`; `spark-r3-master.log` SHA-256 `1819659ce4d71fe7a893aa349564f656ffe2996c08a890f16bd9d20de11ab569`. These are local preflight diagnostics, not GKE evidence. No `run_manifest.json`, Spark table counts, validation, Great Expectations, Trino, executive-mart, generator-vs-Spark, or dbt-vs-Spark report exists for r3. |
| Cleanup and recovery | `rtk make down-batch` returned `0`; only the two pre-existing unrelated containers remained (`frosty_heisenberg` and `nervous_goldberg`). No Docker prune or unrelated-container stop was performed. The backed-up canonical DuckDB and dbt target/log artifacts were restored. DuckDB backup and restored file both hash `dd7fe15130f6375b9a5139e013780121268c386c1e6b0111675ab0b5d46ad9fd`; target file inventories match at 301 files and logs match at 1 file. The explicit remediation backup directory remains available until handoff review. |
| Final acceptance | Focused `rtk uv run pytest tests/unit/test_spark_batch_runtime.py tests/unit/test_optional_duckdb_imports.py -q` returned `0` with `36 passed`. The first full-suite attempt returned `1` (`496 passed, 1 skipped, 1 transient Windows `PermissionError` during a temporary evidence-directory rename`); the isolated failing test then returned `0` with `1 passed`, and the final full `rtk uv run pytest -q` returned `0` with `497 passed, 1 skipped`. `rtk git diff --check` returned `0`. The final index comparison returned `0` with `FC: no differences encountered`; no files were staged; expected implementation edits remain unstaged and the index is unchanged. |
| Screenshot QA | None created or required. Machine evidence is authoritative; successor topics own final UI captures. |
| Limitations and rubric disposition | Topic 04 remains **Partial**. No Spark/Iceberg output parity, three-way report, local Kind result, GKE evidence, final UI evidence, or E34 promotion credit is claimed. This topic supplies supporting local evidence for `Sheet3!E34`; Topic 07 remains the sole primary owner. |
| Successor handoff and stop condition | Topic 05 remains blocked on the strict runtime gate. Preserve the new candidate and failed r3 diagnostics. A successor must run the exact Section 03 window in a fresh evidence root with supported Spark process capacity, then require all 26 Gold relations, all seven ordered DP3 outputs, zero mismatches in `generator_vs_dbt`, `generator_vs_spark`, and `dbt_vs_spark`, and successful validation/Trino/mart checks before changing this status to Complete. The bounded Spark retry is exhausted; no further retry or speculative tuning was performed in this session. |

### Final execution update - 2026-08-04

The update below is authoritative for this execution. The older table rows that follow are retained as historical pre-remediation diagnostics and are superseded where they conflict with this update.

| Field | Final record |
|---|---|
| Status | **Partial.** The Bosch CA image, MinIO proxy contract, dbt contracts, complete 26-table Gold graph, Section 02 evidence, and generator/dbt rebaseline are repaired and locally verified. Both identical bounded strict Spark attempts ended with the executor killed (exit 143) before the `r2` evidence root was published. Generator-vs-dbt still has one semantic training-row mismatch. No three-way parity pass is claimed. |
| Assumptions verified | Branch stayed `feature/implement-edai2`; no branch/worktree/index mutation, commit, push, PR, Kubernetes, Kind, GCP, or cloud-runtime action occurred. The initial pre-existing Topic 04 Completion Record modification was preserved. The initial original Section 03 hash `ece171c3d400c3b16fc668cd28e3587faebe1f596dd6c0c4498ff055e3fc027f` matched before the explicitly authorized Topic 03 serialization rebaseline; the final locked source hash is recorded below. |
| Affected files and interfaces | Topic 03 scope gate: `src/vina_bim_shop/generators/drift_evidence.py`, `tmp/edai2-plan/03_data_generator_improvement.md`, `tmp/edai2-plan/execution-v1/local/03-section03-dbt-gold-contracts.md`, the new Section 03 candidate pointer/bundle, and regenerated Section 01 evidence. Topic 04/runtime: `infra/lakehouse/minio/create-buckets.sh`, `infra/analytics/dbt/models/gold/feat_stream_60m.sql`, `infra/analytics/dbt/tests/assert_training_features_are_point_in_time.sql`, `src/vina_bim_shop/lakehouse/spark/sql.py`, `src/vina_bim_shop/lakehouse/spark/runner.py`, `scripts/analytics/run_section03_dbt.py`, and focused tests. Section 02 maintenance: `scripts/README.md`, `scripts/qa/generate_section02_evidence.py`, `scripts/qa/build_mini_coursework_rubric_manifest.py`, the three Gold diagrams, regenerated Section 02/final-integration evidence, and their tests. The CA Dockerfile/certificate were verified but not changed. Existing public Spark interfaces and the default DP3 selector were preserved; `--all-gold` is explicit internal strict-runtime mode. |
| Ordered execution and results | Generator command `rtk uv run python scripts/generate/run_generator.py --scale medium --mode full --seed 42` returned outer-shell exit `124` after the child continued and published the bundle; verifier `rtk uv run python scripts/generate/verify_section03_manifest.py --manifest evidence/03_data_generator_improvement/section03_candidate_manifest.json --allow-runtime-pending` returned `0` (`PASS`, runtime pending). The default dbt wrapper first returned `1` because the singular point-in-time test still lacked the lower 60-minute predicate (`8,991` rows); after the minimum correction it returned `0`, `PASS=88 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=88`. Explicit `--all-gold` returned `0`, `PASS=133 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=133`; a read-only DuckDB inventory returned exactly 26 required Gold base tables with no missing, unexpected, or duplicate names. The MinIO contract/runtime tests and focused Topic 04 suite returned `0` (`51 passed`). `rtk make up-batch` returned `0`; `minio-init` exited successfully and required services became healthy. Section 02 evidence generation returned `0`; the first manifest-builder diagnostic returned `1` because measured dbt test count was 90 rather than stale 66, then the corrected builder returned `0` with all 45 rubric checks satisfied; verification returned `0`; physical Gold PNG rendering returned `0`; Section 02 focused tests returned `0` (`30 passed`). CA revalidation build returned `0`; the in-image CA/Maven probe returned `0`; host `openssl.exe` returned `1` because OpenSSL is unavailable on Windows; image fingerprint verification returned `0`. The exact strict `run_batch.py` command with the prescribed window was run twice against `tmp/section03-runtime/topic04-spark-gold-20260804-r2`: attempt one returned `1` after about 294 seconds with executor exit 143; the one allowed identical retry returned `1` after about 324 seconds with executor exit 143. No `r2` evidence was published. `rtk make down-batch` returned `0`. Final focused regression returned `0` (`36 passed`); the first full suite returned `1` (`495 passed, 1 skipped, 1 intermittent Windows WinError 5 rename failure`); three fresh manifest-verifier module runs returned `0` (`9 passed` each); final full suite returned `0` (`496 passed, 1 skipped`). |
| Remaining measured issues | Generator-vs-dbt keyed comparison returned zero mismatches for `ml_customer_label`, `agg_feature_health_daily`, and `feature_drift_alerts`. `ml_customer_purchase_training` has one field-mismatched key: `CUS-NTR-00004611`; generator values are `f_stream_add_to_cart_60m=1` and `f_stream_cart_to_purchase_ratio_60m=1.000000000000`, while dbt values are `0` and `0.0`. The raw/staging event trace shows the add-to-cart at 22:59 and order at 23:11; the generator aggregates the lower-bounded 60-minute window across hour buckets, while dbt preserves latest-hour selection. Tolerance was not relaxed and this remains fail-closed. The Spark executor termination prevented generator-vs-Spark and dbt-vs-Spark measurement. |
| Evidence and hashes | Final locked sources: Section 03 `3c906ae30ac0fee606e96608a7b5759cd7439ae048c4c12a84511fce5cc33ae6`, EDAI2 `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`, rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`. Candidate manifest `evidence/03_data_generator_improvement/section03_candidate_manifest.json`: `4af7f8a84da020877bea580f6fc74a9011d371ac2a84a56f7053e83f76246ba1`; bundle ID `1b4123b312da3d1aca70c2dc4f44cf68db248f3b43aad830e8519ca7bbab7fd3`; config `configs/generator/base.yaml`: `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`. Candidate retained hashes: `runs/1b4123.../section03_report.md` `ebdbba5708bbfc8c24006b67204e65a460644bf8539fc4f0ceaf2d90587bd487`; training CSV `603e21581668d9de0147ff1dd58c36fd0a1d0aefed24f237bf6398d816800841`; health CSV `0d9a304b49fdae14aca134a11b8e3420fd202bf093535f1d7904fae8d020963f`; alerts CSV `b656dccc0c86abdf5896e3db1788f04a0fb128e27f2218331f8cc49df188e76a`; labels CSV `526304d4e2d97a70ed6671993b2f5bc07e934dfbd72488cb98c32dcb924f0278`; config snapshot `7405c3e2a27d87be522bea9de7dcc559ad8baff5274a8bed98f002b2753a9196`. Section 02 run manifest `evidence/02_schema_design/run_manifest.json`: `a52cc5fda6e1c4c51af6cfc7ca15e9e2c3fc16337a34336b58b0eff7bbc7f41e`; final rubric manifest `evidence/final_integration/mini_coursework_rubric_manifest.json`: `b753917eafea2867ec6d7f637afd987403f407f9e012137824e268ccd410ca1f`; physical Gold ERD PNG: `7257e13240e9979e6921a5439dd2bc32da212ef7d7586b634aeff2ce6bc50dc4`; updated PlantUML: `7f63df11487dc3b78b9ee62ea79a36a5772aee23a57ede3a259f57b840a6f5a1`. The new Spark `r2` root has no retained files or hashes because the executor was killed before publication; the prior `topic04-spark-gold-20260804` diagnostics remain immutable. |
| Screenshot QA | No Topic 04 screenshot was created or required; machine evidence is authoritative. Section 02 regenerated diagram screenshots are maintenance artifacts, not Spark/GKE evidence. Successor topics own final UI captures. |
| Cleanup and runtime release | Session-created `vina-bim-shop` containers, network, and volumes were released by `rtk make down-batch` with exit `0`; no Docker prune was run. All five pre-existing Docupedia MCP containers remained running. The regenerated complete Gold DuckDB/dbt graph and Section 02 evidence were intentionally retained for the approved Section 02 maintenance scope; the previous candidate bundle remains recoverable. No Kubernetes, Kind, GCP, or cloud runtime state was accessed or mutated. |
| Limitations | No Spark/Iceberg table counts, Great Expectations, Trino, executive-mart, generator-vs-Spark, or dbt-vs-Spark evidence exists for this continuation because the local executor was killed twice before publication. Resolving the one generator-vs-dbt semantic mismatch requires an explicitly authorized decision to align hour-bucket/latest-hour semantics or to aggregate the full lower-bounded 60-minute window in both implementations. This local result is not GKE evidence, final UI evidence, or E34 promotion evidence. |
| Rubric disposition | Supporting local evidence only for `Sheet3!E34`; Topic 07 remains the sole primary owner. Topic 04 remains `Partial`; local dbt/Section 02/CA results do not establish strict runtime or promotion credit. |
| Stop conditions | The one bounded Spark retry was exhausted after executor exit 143, and the one-row generator/dbt mismatch remained. No further retry, speculative tuning, certificate import, insecure TLS flag, Docker prune, unrelated-container stop, Kubernetes command, or GCP mutation was performed. |
| Successor handoff | Topic 05 remains blocked on the strict runtime gate. Obtain explicit scope for the remaining stream-semantic choice, then run the exact medium-window command in a fresh evidence root using bundle `1b4123b312da3d1aca70c2dc4f44cf68db248f3b43aad830e8519ca7bbab7fd3` and config SHA `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`, with sufficient supported Spark runtime capacity. Require all 26 Gold relations, all seven ordered DP3 outputs, zero mismatches in all three parity families, and successful validation/Trino/mart evidence before considering Complete; Topic 07 owns any later GKE evidence. |

| Field | Record |
|---|---|
| Status | **Partial — The Bosch CA image, MinIO proxy contract, dbt contracts, complete 26-table Gold graph, Section 02 evidence, and generator/dbt rebaseline are repaired and locally verified. Strict local Spark/Iceberg execution remains incomplete: both identical bounded Spark attempts ended with the executor killed (exit 143) before the `r2` evidence root was published. The direct generator-vs-dbt check still has one semantic training-row mismatch, so no three-way parity pass is claimed.** |
| Affected files | Modified only this Completion Record during this continuation. The existing Topic 04 source/test files and the pre-existing CA Dockerfile, certificate, and CA contract test were inspected but not changed; no dbt, generator, orchestration, Kubernetes, or GCP file changed. Session-owned diagnostic evidence is under `tmp/section03-runtime/topic04-spark-gold-20260804/`. |
| Commands / exit codes | `rtk git status --short --branch` → `0`, unchanged `feature/implement-edai2`; all three locked-source `rtk certutil.exe -hashfile ... SHA256` checks → `0` with exact matches; pre-edit `rtk git ls-files --stage > C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-index-before.txt` → `0`. Candidate/config hashes → `0`, exact expected values. `rtk uv run pytest tests/unit/test_spark_dockerfile_ca.py -q` → `0` (`2 passed`); `rtk uv run pytest tests/unit/test_spark_batch_runtime.py -q` → `0` (`33 passed`). `rtk docker build --progress=plain -f infra/spark/Dockerfile -t vina-bim-shop-spark:ca-fixed .` → `0`; exact in-image CA/Maven probe → `0`; host `rtk openssl.exe ...` → `1` because OpenSSL is unavailable on the Windows host, substituted image OpenSSL fingerprint check → `0` and matched `BE:FE:A4:B1:F8:75:4F:18:C4:49:A0:D5:50:FF:E7:6D:AB:A6:09:F4:10:B6:5F:ED:1B:B4:06:4A:A6:D9:A3:06`. Direct dbt wrapper → `0`, `PASS=88 WARN=0 ERROR=0 SKIP=0 NO-OP=0 TOTAL=88`. Read-only inventory/parity diagnostic → `0`: `13/26` required Gold relations present, `13` missing, all seven DP3 present, and `ml_customer_purchase_training` has `9,592` field mismatches while the other three keyed checks have zero mismatches. Initial `rtk make up-batch` → `1` at `minio-init` HTTP 502; proxy-disabled `rtk docker compose --profile lakehouse --profile batch run --rm --no-deps -e HTTP_PROXY= -e HTTPS_PROXY= -e http_proxy= -e https_proxy= -e ALL_PROXY= -e all_proxy= -e NO_PROXY=minio,localhost,127.0.0.1 -e no_proxy=minio,localhost,127.0.0.1 minio-init` → `0`; one bounded proxy-disabled `rtk make up-batch` retry → `1` with the same `minio-init` dependency failure. `rtk make down-batch` → `0`; post-cleanup Docker check → `0`, no `vina-bim-shop` volumes remained. Predecessor DuckDB/dbt artifacts were restored and their original hashes reverified. Final `rtk uv run pytest tests/unit/test_spark_batch_runtime.py tests/unit/test_optional_duckdb_imports.py -q` → `0` (`35 passed`); final `rtk git diff --check` → `0`; final `rtk git diff --name-only` → `0` with only this Completion Record; final `rtk git status --short --branch` → `0` with only this Completion Record modified; final `rtk git ls-files --stage > C:\Users\oou1hc\AppData\Local\Temp\edai2-topic04-index-after.txt` → `0`; `rtk fc.exe /b ...` → `0`, `FC: no differences encountered`. Full `rtk uv run pytest -q` → `1` after `882.66s` (`483 passed, 1 skipped, 6 failed`); the failures are unrelated baseline checks listed under Limitations. |
| Evidence + SHA256 | Locked sources: Section03 `ece171c3d400c3b16fc668cd28e3587faebe1f596dd6c0c4498ff055e3fc027f`, EDAI2 `b8be3ef5c84fe4d6fe52e8894c3c5dc1c3babc898e2a87684b2b8ff720d6d079`, rubric workbook `71b2403e068081b00245bea5e15c5754f3762ad354e0a3a6576d69e4963c8657`. Candidate manifest `evidence/03_data_generator_improvement/section03_candidate_manifest.json`: `8c0ff22ef528f4170506734f03c055aeec742e62bd9e94d7d0f703e47653bed7`; bundle `c220ab9dc815fb6ccff187b661a0fa483f8c16bcda368dd214a0f78633d6c92f`; bound config `configs/generator/base.yaml`: `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`. Current diagnostic `tmp/section03-runtime/topic04-spark-gold-20260804/dbt-inventory-and-generator-parity.txt`: `2b97246ebb15d0a7879b7e5e01c2997f17b183c85e8b443664290a64f690deaf`; initial Compose log `make-up-batch-initial.log`: `70e825dd518559cdcef481d9f0b3e1de615257a2053dd74350451436fc97e40d`; proxy-retry log `make-up-batch-proxy-retry.log`: `b636b543ddf0a1bc62242edcebebe7def84d2727e9665b84696a799adb9236bf`; `minio-init-retry.log`: `5b2822bfeb58c1ac351db774811339728e9ef67a4e47da44182fcd1f5a3d7b83`; Docker state capture: `5f90d34046ce220265fe384e3c6da48af3ac7fd6e726c7de502b53058683e042`. The Docupedia SHA256SUMS attachment reports the DER/certificate fingerprint `BEFE...`; the authoritative downloaded PEM raw hash was `46d26ce24971172304c94bd5d5308866dc394391edef3af55d20282e8a2f1aec`, while the repository PEM raw hash was `f84fff2fa94b68f4cf255db1720e03874f7e52b43078b52c817ea450e8cab3ff`; normalized PEM bytes were identical and DER hashes matched. |
| Screenshot QA | `No screenshot created or required; machine evidence only. Successor topics own final UI captures.` |
| Cleanup / runtime release | The session-created `vina-bim-shop` containers, network, and volumes were released by `rtk make down-batch` → `0`; no Docker prune was run. The six pre-existing Docupedia/Playwright containers were left running. The predecessor DuckDB and dbt artifacts were restored and reverified at DuckDB `52f70910d274b0c188713c2b6e1cd76c12b63102225e09652ffa0479c902d6ea`, manifest `0cef60444ab4c5208e3381df737b34975a4e480759e3f227f566b4d88048ecc7`, run results `aadfb7ebb1e2c7f35ed4be5ad62d746c5ce104b5b710c4b22c73578700c622ab`, and dbt log `77c316ec0c7729fae12ead1c3f1cea3976f2d49cdd27a6d148ab364cd897c49d`. No Kubernetes/GCP resource was accessed or mutated. |
| Limitations | The strict `run_batch.py` command was not executed because the only local Compose runtime slice could not satisfy its `minio-init` dependency after one bounded retry. Consequently, no Spark/Iceberg table counts, Trino smoke, Great Expectations result, generator-vs-Spark report, or dbt-vs-Spark report exists for this continuation. The current dbt output exposes only 13 of 26 required Gold relations and disagrees with the locked generator on 9,592 training rows; Topic 04 does not authorize upstream dbt/generator edits, so parity remains fail-closed. The full suite also retained six unrelated baseline failures: `test_scripts_readme_documents_every_tracked_script`, `test_all_zone_model_inventory_and_metadata_match_schema_design_source`, `test_physical_gold_model_puml_documents_all_layers_and_purposes`, `test_duckdb_gold_tables_have_physical_constraints_for_dbeaver_erd`, `test_promotion_sentinel_makes_fact_order_item_promotion_key_non_null`, and `test_schema_design_puml_shows_storage_and_serving_contracts`. The CA image is locally verified but this does not establish Spark output parity, GKE behavior, final UI evidence, or E34 runtime/promotion credit. |
| Rubric disposition | Supporting evidence only for `Sheet3!E34`; Topic 07 remains the sole primary owner. Local unit/dbt/CA results do not earn strict runtime or promotion credit, and no GKE claim is made. |
| Stop conditions | Triggered by the Compose `minio-init` proxy/502 failure and the persistent upstream required-inventory/training mismatch. One bounded proxy-disabled retry was exhausted; no further tuning, speculative certificate import, Docker prune, unrelated-container stop, Kubernetes command, or GCP mutation was performed. |
| Successor handoff | Topic 05 remains blocked on this gate. Before promotion, repair or explicitly authorize the Compose proxy/no-proxy runtime configuration and the upstream dbt/generator contract scope; rebuild the full 26-table Gold inventory from candidate `c220ab9dc815fb6ccff187b661a0fa483f8c16bcda368dd214a0f78633d6c92f` and config SHA `5c5029fea77d93c3941d87d9a60f85cfe374f36899c3d3c1b028f3f5f3c3cd97`; rerun the exact medium-window batch command, real Spark/Iceberg/Trino checks, and all three keyed comparison families; replace this diagnostic failure evidence only with a zero-mismatch report. |
